{
  "name": "gsl-parser-lambda",
  "version": "1.0.0",
  "description": "Lambda function to parse .gsl files from gslist or ealist",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "cetteup (me@cetteup.com)",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.874.0"
  },
  "devDependencies": {}
}
